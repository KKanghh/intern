import styled from "styled-components";

const Title = styled.div`
  font-family: "Apple SD Gothic Neo", "Noto Sans KR", sans-serif;
  font-size: 25px;
  font-weight: 900;
  letter-spacing: -1px;
  line-height: 32px;
  margin-bottom: 12px;
  margin-left: 15px;
`;

const NoticeIDComponents = {
  Title,
};

export default NoticeIDComponents;
